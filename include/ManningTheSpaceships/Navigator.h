#ifndef Navigator_H
#define Navigator_H
#include "ManningTheSpaceships/Human.h"


class Navigator : public Human{
private:
    /* data */
public:
    Navigator(/* args */);
    ~Navigator();
    /*
    must still add communication stuff
    */
    
};

#endif